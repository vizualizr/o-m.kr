---
import { getCollection } from "astro:content";
import SwissLayout from "@layouts/swiss/SwissLayout.astro";
import Patchwork from "@components/swiss/Patchwork.astro";
import { patchworkMapper } from "@utils/swiss/patchwork-logic";

const allArticles = await getCollection("articles");
const mappedArticles = patchworkMapper(allArticles);

// The prototype uses Anton, Lora, and Noto Sans KR.
// These are imported in global.css.
---

<SwissLayout title="o-m.kr | Swiss Patchwork">
  <div class="relative z-10 min-h-screen flex flex-col items-center w-full">
    <div class="fixed inset-0 pointer-events-none z-0">
      <div class="h-full w-full mx-auto border-x border-outline relative"></div>
    </div>

    <header
      class="w-full flex justify-center border-b border-outline bg-background-light/90 dark:bg-background-dark/90 backdrop-blur-xl sticky top-0 z-50"
    >
      <div
        class="max-w-[1400px] w-full flex items-center justify-between px-8 py-6"
      >
        <div class="flex items-center gap-4 text-base-content relative">
          <div class="grid-dot -top-[25px] -left-[9px]"></div>
          <h2
            class="font-display text-4xl font-normal uppercase tracking-tight"
          >
            O-M.KR
          </h2>
          <span
            class="bg-primary text-primary-content text-[10px] px-2 py-0.5 rounded font-bold uppercase tracking-widest ml-2"
          >
            Swiss Patchwork v1
          </span>
        </div>
        <div class="flex items-center gap-12">
          <nav
            class="hidden md:flex items-center gap-10 uppercase text-[10px] font-bold tracking-[0.2em] text-base-content"
          >
            <a class="hover:opacity-60 transition-opacity" href="#">이야기</a>
            <a class="hover:opacity-60 transition-opacity" href="#">아카이브</a>
            <a class="hover:opacity-60 transition-opacity" href="#">임팩트</a>
          </nav>
          <button
            class="bg-primary text-white text-[10px] font-bold uppercase tracking-[0.2em] px-8 py-3 rounded-full hover:scale-105 transition-transform"
          >
            연결하기
          </button>
        </div>
      </div>
    </header>

    <main class="w-full flex-1 border-1 mx-0">
      <section class="p-8 md:p-16 border-b border-outline hidden">
        <div class="">
          <span
            class="text-[10px] font-bold uppercase tracking-[0.5em] text-primary/50 dark:text-white/40 block mb-6 italic"
            >제 24호 — 01</span
          >
          <h1
            class="korean-condensed-block text-7xl md:text-[140px] text-base-content mb-12"
          >
            근본적<br />명료함.
          </h1>
          <p
            class="text-base-content/80 text-xl md:text-2xl max-w-2xl leading-relaxed font-light"
          >
            스위스 기능주의와 엄격한 수학적 시각화를 통해 사회의 시스템적 패턴을
            탐구합니다.
          </p>
        </div>
      </section>

      <section class="patchwork-grid">
        {mappedArticles.map((article) => <Patchwork article={article} />)}
      </section>
    </main>

    <footer
      class="w-full max-w-[1400px] border-t border-outline py-24 px-8 mt-auto"
    >
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-16">
        <div class="col-span-1 md:col-span-2">
          <h2 class="korean-condensed-block text-5xl text-base-content mb-8">
            O-M.KR 데이터 스토리텔링
          </h2>
          <p
            class="text-[10px] font-bold uppercase tracking-[0.4em] text-primary/40 dark:text-white/40"
          >
            스위스 취리히 • 2024년 설립<br />데이터 엄밀성을 통한 사회적 임팩트
          </p>
        </div>
        <div>
          <span
            class="text-[9px] font-bold uppercase tracking-[0.2em] text-primary/30 block mb-6"
            >디렉토리</span
          >
          <nav
            class="flex flex-col gap-4 uppercase text-[10px] font-bold tracking-widest text-primary/70 dark:text-white/70"
          >
            <a
              class="hover:text-primary dark:hover:text-white transition-colors"
              href="#">저장소</a
            >
            <a
              class="hover:text-primary dark:hover:text-white transition-colors"
              href="#">임팩트 보고서</a
            >
            <a
              class="hover:text-primary dark:hover:text-white transition-colors"
              href="#">윤리 강령</a
            >
          </nav>
        </div>
        <div>
          <span
            class="text-[9px] font-bold uppercase tracking-[0.2em] text-primary/30 block mb-6"
            >소셜</span
          >
          <div class="flex gap-6">
            <span
              class="material-symbols-outlined text-base-content cursor-pointer hover:opacity-50 transition-opacity"
              >alternate_email</span
            >
            <span
              class="material-symbols-outlined text-base-content cursor-pointer hover:opacity-50 transition-opacity"
              >rss_feed</span
            >
            <span
              class="material-symbols-outlined text-base-content cursor-pointer hover:opacity-50 transition-opacity"
              >terminal</span
            >
          </div>
        </div>
      </div>
      <div
        class="mt-24 pt-8 border-t border-outline/30 flex justify-between items-center text-[9px] font-bold uppercase tracking-[0.3em] text-primary/30"
      >
        <span>© 2024 o-m.kr</span>
        <span>개인정보 처리방침 및 거버넌스</span>
      </div>
    </footer>
  </div>
</SwissLayout>

<style>
  /* Any page-specific overrides can go here */
  body {
    background-color: transparent !important; /* Let the internal div handle it if Paper has a background */
  }
</style>
