# Google Sheets CMS Strategy Test Plan

기존 로컬 아티클의 메타데이터를 구글 시트로 푸시하여 CMS 전략의 실현 가능성을 검증합니다.

## User Review Required

> [!IMPORTANT]
> - **의존성 설치**: `googleapis` (구글 API), `gray-matter` (마크다운 파싱) 설치가 필요합니다.
> - **인증 파일**: 구글 서비스 계정 키(`JSON`)를 제공해 주시면 `.agent/secrets/google-keys.json` 경로에 저장하여 사용하겠습니다. (해당 폴더는 [.gitignore](file:///d:/yonggeun/porter/git/o-m.kr/production/.gitignore)에 추가하겠습니다.)

## Proposed Changes

### [Component] Data Pipeline Scripts

#### [NEW] [sync-to-sheets.js](file:///d:/yonggeun/porter/git/o-m.kr/production/scripts/sync-to-sheets.js)
- 로컬 `src/content/article/` 폴더를 순회하며 모든 `.md`, `.mdx` 파일의 프론트매터를 추출합니다.
- 파일 경로를 기반으로 `UID`를 자동 생성(또는 추출)합니다.
- 구글 시트 API를 통해 해당 데이터를 지정된 시트에 업로드합니다.

#### [MODIFY] [.gitignore](file:///d:/yonggeun/porter/git/o-m.kr/production/.gitignore)
- `.agent/secrets/` 폴더를 제외 목록에 추가하여 인증 정보가 유출되지 않도록 합니다.

## Verification Plan

### Automated Steps
- `node scripts/sync-to-sheets.js` 명령을 실행하여 데이터 전송 성공 여부 확인.

### Manual Verification
- 구글 시트에서 업로드된 데이터(headline, slug, UID 등)가 로컬 파일과 일치하는지 확인.
